export const metadata = {
  title: "LaunchNYC — Your NYC apartment search, simplified",
  description: "Save listings, track applications, and get your documents in order. Sign up or log in to get started.",
};

export default function LandingLayout({ children }) {
  return children;
}
